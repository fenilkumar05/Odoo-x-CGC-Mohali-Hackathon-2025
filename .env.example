# QuickDesk Enhanced Edition - Environment Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# CORE APPLICATION SETTINGS
# =============================================================================

# Secret key for Flask sessions and CSRF protection
# IMPORTANT: Generate a unique secret key for production!
# You can generate one using: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-this-in-production

# Database configuration
# For development (default): SQLite database
DATABASE_URL=sqlite:///quickdesk.db

# For production, use PostgreSQL or MySQL:
# DATABASE_URL=postgresql://username:password@localhost/quickdesk
# DATABASE_URL=mysql://username:password@localhost/quickdesk

# =============================================================================
# EMAIL CONFIGURATION (REQUIRED)
# =============================================================================

# SMTP server settings for email notifications
# Gmail example (recommended for testing):
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Outlook/Hotmail example:
# MAIL_SERVER=smtp-mail.outlook.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@outlook.com
# MAIL_PASSWORD=your-password

# Custom SMTP server example:
# MAIL_SERVER=your-smtp-server.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-username
# MAIL_PASSWORD=your-password

# Default sender email (optional, uses MAIL_USERNAME if not set)
MAIL_DEFAULT_SENDER=your-email@gmail.com

# =============================================================================
# FILE UPLOAD CONFIGURATION
# =============================================================================

# Directory for uploaded files (relative to project root)
UPLOAD_FOLDER=uploads

# Maximum file size in bytes (16MB = 16777216 bytes)
MAX_CONTENT_LENGTH=16777216

# Allowed file extensions (comma-separated)
ALLOWED_EXTENSIONS=txt,pdf,png,jpg,jpeg,gif,doc,docx,xls,xlsx,zip

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Flask environment (development/production)
FLASK_ENV=development

# Enable/disable debug mode (set to False in production)
FLASK_DEBUG=True

# Application host and port
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# =============================================================================
# SECURITY SETTINGS
# =============================================================================

# Session configuration
PERMANENT_SESSION_LIFETIME=3600

# CSRF token expiration (in seconds)
WTF_CSRF_TIME_LIMIT=3600

# =============================================================================
# OPTIONAL FEATURES
# =============================================================================

# Pagination settings
TICKETS_PER_PAGE=20
USERS_PER_PAGE=20

# Notification settings
ENABLE_EMAIL_NOTIFICATIONS=True
ENABLE_BROWSER_NOTIFICATIONS=True

# =============================================================================
# PRODUCTION SETTINGS (Uncomment for production deployment)
# =============================================================================

# Production database (uncomment and configure for production)
# DATABASE_URL=postgresql://username:password@localhost/quickdesk_prod

# Production email service (uncomment for production email service)
# MAIL_SERVER=smtp.sendgrid.net
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=apikey
# MAIL_PASSWORD=your-sendgrid-api-key

# Redis configuration (for caching and background tasks)
# REDIS_URL=redis://localhost:6379/0

# Celery configuration (for background tasks)
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log file path (optional, logs to console if not set)
# LOG_FILE=logs/quickdesk.log

# =============================================================================
# NOTES
# =============================================================================

# 1. Gmail App Passwords:
#    - Enable 2-factor authentication on your Gmail account
#    - Generate an app password: https://myaccount.google.com/apppasswords
#    - Use the app password instead of your regular password

# 2. Security Best Practices:
#    - Always change the SECRET_KEY in production
#    - Use environment-specific configuration files
#    - Never commit real credentials to version control
#    - Use strong passwords and enable 2FA where possible

# 3. File Upload Security:
#    - Limit file sizes to prevent abuse
#    - Restrict file types to safe extensions
#    - Scan uploaded files for malware in production

# 4. Database Considerations:
#    - SQLite is suitable for development and small deployments
#    - Use PostgreSQL or MySQL for production environments
#    - Regular database backups are recommended

# 5. Email Delivery:
#    - Gmail has daily sending limits for personal accounts
#    - Consider using dedicated email services (SendGrid, Mailgun) for production
#    - Test email configuration before deploying to production
